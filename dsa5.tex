%!TEX engine = lualatex

\usepackage[ngerman]{babel}
% enquotes command
\usepackage[german=guillemets]{csquotes}

% Andalus wird für Überschriften bei DSA 5 Büchern verwendet
\usepackage{fontspec}
\newfontfamily\andalus{andalus}
\addtokomafont{section}{\andalus\fontsize{23.5}{23.5}\selectfont}

% Gentium wird für Fließtext bei DSA 5 Büchern verwendet
\setmainfont[Ligatures=TeX]{Gentium}[
  BoldFont  = Gentium Basic Bold,
  ItalicFont = Gentium Italic]
\addtokomafont{subsection}{\rmfamily\fontsize{13}{13}\selectfont}
\addtokomafont{descriptionlabel}{\rmfamily}

% Fake small caps (https://tex.stackexchange.com/a/225078)
\usepackage{graphicx}
\newcommand\fauxsc[1]{\fauxschelper#1 \relax\relax}
\def\fauxschelper#1 #2\relax{%
  \fauxschelphelp#1\relax\relax%
  \if\relax#2\relax\else\ \fauxschelper#2\relax\fi%
}
\def\Hscale{.83}\def\Vscale{.73}\def\Cscale{1.00}
\def\fauxschelphelp#1#2\relax{%
  \ifnum`#1>``\ifnum`#1<`\{\scalebox{\Hscale}[\Vscale]{\uppercase{#1}}\else%
    \scalebox{\Cscale}[1]{#1}\fi\else\scalebox{\Cscale}[1]{#1}\fi%
  \ifx\relax#2\relax\else\fauxschelphelp#2\relax\fi}

% Angabe des Regelbuches
\newcommand{\quelle}[2]{\noindent\fauxsc{#1}, S. #2}

% Todo command
\usepackage{color}
\newcommand{\todo}[1]{\textcolor{red}{#1}}

% Italic block quote
\newenvironment{itquote}{\begin{quote}\itshape}{\end{quote}}
